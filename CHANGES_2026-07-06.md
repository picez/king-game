# Зміни за сесію — 2026-07-06 (hardening + Durak stats)

Реалізовано **18/29** задач плану з ревізії (див. трекер): повністю закрито блоки
**БЕЗ** (безпека), **I18N** (локалізація), **ТЕСТ** (покриття) і **DURAK**
(статистика). **13 комітів** від `picez`, гілка `main`.

Базовий стан на кінець сесії: `tsc --noEmit` чистий · **610 тестів пройдено**
(+10 skipped — DB-інтеграційні) · `npm run build` OK (104 модулі) · `npm run e2e`
PASS · `npm run soak` 180/180.

> **Верифікація для тебе:** `npm test` (юніт+логіка), `npm run build`
> (типи+прод-збірка), `npm run e2e` (онлайн-флоу), `npm run soak` (боти Durak).
> DB-фічі (статистика Durak): щоб прогнати інтеграційні тести — задай
> `TEST_DATABASE_URL=postgres://…` на мігрованій БД і `npm test`.

---

## 🔒 БЕЗ — безпека сервера (6 задач)

| Коміт | Задача | Що змінилось | Як перевірити |
|-------|--------|--------------|---------------|
| `c27f4db` | БЕЗ-1/2 | Per-connection WS rate-limiting (token buckets: message + CREATE_ROOM) + фікс витоку кімнат (`leaveCurrentSession` на CREATE/JOIN). Env: `WS_MSG_BURST/PER_SEC`, `WS_CREATE_BURST/PER_SEC`. | `rateLimit.test.ts`, `wsHandlers.leak.test.ts`; онлайн: спробувати створити багато кімнат поспіль одним клієнтом → throttle |
| `4d49a1f` | БЕЗ-3 | Пароль кімнати тепер **scrypt** (node:crypto) + constant-time; хешер інжектується (serverCore лишається клієнт-безпечним); tagged `scrypt$…` з fallback на старий KDF | `roomPassword.test.ts`; онлайн: кімната з паролем, хибний пароль відхиляється |
| `cd501f9` | БЕЗ-4 | Reconnect-токени хешуються at rest (SHA-256); plaintext лише транзитно в WELCOME | `reconnectToken.test.ts`; онлайн: reconnect після дропа працює |
| `5d77410` | БЕЗ-5 | OAuth: лінкування Google-акаунта лише за **verified email** (`isLinkableIdentity`) | `googleOAuth.test.ts` |
| `ce79981` | БЕЗ-6 | Brute-force ліміт на join (бюджет невдалих спроб; перевірка **до** пошуку кімнати — без timing oracle) | `rateLimit.test.ts`, `wsHandlers.leak.test.ts` |

## 🌐 I18N — локалізація (5 задач)

| Коміт | Задача | Що змінилось |
|-------|--------|--------------|
| `af38d25` | I18N-1 | `SetupScreen` (локальна гра King) переведено на `useI18n` — був повністю англомовний. +13 ключів × 4 мови |
| `71ab694` | I18N-2..5 | `ScoreBoard` заголовки; помилки `useRoomList` → коди+переклад; wss-попередження; aria-мітки `CardView`/Durak/`ScoreTracker`; **guard-тест паритету** `parity.test.ts` (en/uk/de/ar) |

**Перевірити:** перемкнути мову в Профілі → усі екрани (зокрема локальний setup King) локалізовані; Arabic RTL без overflow на 360/390.

## 🧪 ТЕСТ — покриття (3 задачі)

| Коміт | Задача | Що змінилось |
|-------|--------|--------------|
| `452057b` | ТЕСТ-1/2 | Тести King AI (`ai.test.ts` — легальність ходів у всіх 7 режимах + Trump/Mode/Kitty); `modeQueue.test.ts`, `gameConfigs.test.ts` |
| `526170e` | ТЕСТ-3 | Durak-бот навчено робити **TRANSFER_ATTACK**; `transfer.test.ts` (chaining по колу, bot emits transfer). Soak: 180/180 |

## 🎮 DURAK — статистика (4 задачі) — закриває останній gap DURAK_PLAN

| Коміт | Задача | Що змінилось | Як перевірити |
|-------|--------|--------------|---------------|
| `7eb9727` | DURAK-1 | Запис статистики Durak (`recordsStats: true`; fool/draw). `maybeRecordFinished` тепер game-agnostic (через `getGameDefinition`) | `durakStats.test.ts` + інтеграційний (з БД) |
| `60ff113` | DURAK-2 | `GET /api/games/durak/stats` (session-required) | `apiDisabled.test.ts` (503 без БД) |
| `b65ee78` | DURAK-3 | UI-панель Durak-статистики + перемикач King/Durak у вкладці Stats | `statsApi.test.ts`; UI: Профіль → Статистика → Durak |
| `477b117` | DURAK-4 | Лідерборд Durak (endpoint + панель + перемикач) | UI: Профіль → Лідерборд → Durak |

**Перевірити (потребує БД):** зіграти онлайн-гру Durak до кінця → статистика/лідерборд оновлюються; без `DATABASE_URL` усе м'яко деградує (панелі показують «недоступно»), гра не страждає.

---

## ⏭️ Лишилось (11 задач)

- 🔧 **БОРГ** (19–22) — рефакторинг ядра: дедуплікація penalty-предиката, декомпозиція `handlePlayCard`, a11y модалок (Esc/role=dialog), мемоізація `GameContext`. Верифіковно локально.
- ✅ **РЕЛ** (23–25) — **ручне**: QA на реальних телефонах (23), deploy HTTPS/WSS (25); soak (24) автоматизовний.
- 🧩 **SEAM** (26–28) + **ГРА-N** (29) — підготовка платформи до третьої гри.

## Примітки

- Файли, що були модифіковані **до** цієї сесії (реліз Durak 9.13, частина i18n/UI, `MVP_STATUS.md` тощо), лишилися незакоміченими — я їх **не** чіпав, щоб не змішувати з роботою над цим планом. Розберися з ними окремо.
- Усі коміти від `picez <picezzzzzz@gmail.com>`, з trailer-ами Claude Code.
